# Telegram userbot MCP proxy — pinned upstream + HTTP serving deps.
# telethon, mcp[cli], and nest-asyncio arrive transitively from telegram-mcp.
# telegram-mcp v3.2.0 is fetched from its immutable release commit.
telegram-mcp @ https://github.com/chigwell/telegram-mcp/archive/f1a2d8e00a7f127bb7702655c58fdfcee7e73a5a.tar.gz
uvicorn>=0.30
starlette>=0.37
# Telethon's SOCKS support is optional upstream; production needs it when direct
# MTProto egress is blocked and the userbot is routed through the local VPN.
python-socks>=2.7,<3
